[package]
name = "webhook-ingest"
description = "Webhook signature verification + dedup, composing idempotency:guard (webhook:ingest)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen-rt = { workspace = true }
hmac = { workspace = true }
sha2 = { workspace = true }

[package.metadata.component]
package = "webhook:component"

[package.metadata.component.target]
path = "wit"
world = "webhook-ingest"

[package.metadata.component.target.dependencies]
"idempotency:guard" = { path = "../idempotency-guard/wit" }
"wasi:keyvalue" = { path = "../../wit/deps/wasi-keyvalue-0.2.0-draft" }
"wasi:clocks" = { path = "../../wit/deps/wasi-clocks-0.2.0" }
"wasi:random" = { path = "../../wit/deps/wasi-random-0.2.0" }
"wasi:config" = { path = "../../wit/deps/wasi-config-0.2.0-draft" }
"wasi:io" = { path = "../../wit/deps/wasi-io-0.2.0" }
