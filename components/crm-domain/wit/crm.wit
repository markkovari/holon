// crm:pipeline — contacts and deals through a sales pipeline.
//
// Composed from existing capabilities, not new capability code: auth:identity
// for real accounts/login/roles, policy:guard for the row-level rule RBAC
// cannot express ("a rep sees only their own deals"), records:store for
// storage, audit:log for a compliance trail, and rate-limiter for
// login/register brute-force protection — the same shape
// `ticket-triage-domain`/`track-domain` are built from.
package crm:pipeline@0.1.0;

world crm-domain {
    export wasi:http/incoming-handler@0.2.0;

    import auth:identity/accounts@0.1.0;
    import auth:identity/authorizer@0.1.0;
    import auth:identity/session@0.1.0;
    import auth:identity/rbac@0.1.0;
    import auth:identity/types@0.1.0;

    import policy:guard/guard@0.1.0;
    import records:store/store@0.1.0;
    import audit:log/recorder@0.1.0;
    import ratelimit:guard/limiter@0.1.0;

    import wasi:clocks/wall-clock@0.2.0;
    import wasi:http/outgoing-handler@0.2.0;
}
