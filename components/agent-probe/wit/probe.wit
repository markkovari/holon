// comp:agentprobe — an instrument for `graph:agent`, not a feature.
package comp:agentprobe@0.1.0;

world agent-probe {
    export wasi:http/incoming-handler@0.2.0;
    import graph:agent/writer@0.1.0;
}
