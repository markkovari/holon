node_modules/
gen/
# the component is a build artifact; regenerate with `cargo xtask build --force` + componentize,
# or copy from the components/ build output. Kept out of git like other .wasm.
auth_guard.wasm
