# FULL vet-clinic on wasmCloud (generated by gen-manifest.py).
# HYBRID topology: the 6 pure-compute caps are wac-fused INTO vet-domain
# (28 core modules, under the 30 limit); stateful caps stay wadm links. The host runs all components + their links, well within its
# multi-thousand instance density.
apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: vet-domain
  namespace: vet-clinic
  annotations:
    version: v0.2.0-lattice-r5
    description: "Full vet-clinic, hybrid fuse+link topology, on wasmCloud k8s"
spec:
  components:
    - name: vet-domain
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-vet-domain-lattice:0.1.0
        config:
          - name: vet-domain-config
            properties:
              allowed-types: "image/png,image/jpeg,image/webp,image/gif"
              cursor-secret: "vet-cursor-secret"
              max-page-size: "100"
              max-size: "2097152"
              ticket-secret: "vet-upload-secret"
              ticket-ttl: "300"
      traits:
        - type: spreadscaler
          properties:
            instances: 5
        - type: link
          properties:
            namespace: auth
            package: identity
            interfaces: [accounts, session, authorizer, rbac, types]
            target:
              name: auth-guard
        - type: link
          properties:
            namespace: records
            package: store
            interfaces: [store]
            target:
              name: records-store
        - type: link
          properties:
            namespace: search
            package: index
            interfaces: [index]
            target:
              name: search-index
        - type: link
          properties:
            namespace: blob
            package: store
            interfaces: [blobstore]
            target:
              name: blob-store
        - type: link
          properties:
            namespace: fsm
            package: workflow
            interfaces: [engine]
            target:
              name: fsm-workflow
        - type: link
          properties:
            namespace: csv
            package: codec
            interfaces: [codec]
            target:
              name: csv
        - type: link
          properties:
            namespace: otp
            package: totp
            interfaces: [authenticator]
            target:
              name: otp
        - type: link
          properties:
            namespace: secrets
            package: vault
            interfaces: [vault]
            target:
              name: secrets-vault
        - type: link
          properties:
            namespace: i18n
            package: catalog
            interfaces: [catalog]
            target:
              name: i18n-catalog
        - type: link
          properties:
            namespace: ai
            package: inference
            interfaces: [inference]
            target:
              name: ai-inference
        - type: link
          properties:
            namespace: cache
            package: store
            interfaces: [cache]
            target:
              name: cache
        - type: link
          properties:
            namespace: sched
            package: timer
            interfaces: [timer]
            target:
              name: scheduler-timer
        - type: link
          properties:
            namespace: lock
            package: mutex
            interfaces: [mutex]
            target:
              name: lock-mutex
        - type: link
          properties:
            namespace: event
            package: bus
            interfaces: [bus]
            target:
              name: event-bus
        - type: link
          properties:
            namespace: ui
            package: assets
            interfaces: [files]
            target:
              name: static-assets
    - name: auth-guard
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-auth-guard:0.1.0
        config:
          - name: auth-guard-config
            properties:
              default-tenant: "acme-vet"
              session-ttl: "3600"
              password-min-len: "8"
              audit-enabled: "true"
              max-attempts: "5"
              lockout-window: "300"
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: authguard-bucket
                  properties:
                    bucket: authguard
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
        - type: link
          properties:
            namespace: wasi
            package: http
            interfaces: [outgoing-handler]
            target:
              name: httpclient
    - name: records-store
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-records-store:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: recordsstore-bucket
                  properties:
                    bucket: recordsstore
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: search-index
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-search-index:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: searchindex-bucket
                  properties:
                    bucket: searchindex
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: blob-store
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-blob-store:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: blobstore-bucket
                  properties:
                    bucket: blobstore
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: fsm-workflow
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-fsm-workflow:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: fsmworkflow-bucket
                  properties:
                    bucket: fsmworkflow
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: csv
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-csv:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
    - name: otp
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-otp:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: otp-bucket
                  properties:
                    bucket: otp
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: secrets-vault
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-secrets-vault:0.1.0
        config:
          - name: secrets-vault-config
            properties:
              master-key: "dmV0LWNsaW5pYy1kZW1vLW1hc3Rlci1rZXktMzJiISE="
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: secretsvault-bucket
                  properties:
                    bucket: secretsvault
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: i18n-catalog
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-i18n-catalog:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: i18ncatalog-bucket
                  properties:
                    bucket: i18ncatalog
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: ai-inference
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-ai:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
    - name: cache
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-cache:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: cache-bucket
                  properties:
                    bucket: cache
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: scheduler-timer
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-scheduler-timer:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: schedulertimer-bucket
                  properties:
                    bucket: schedulertimer
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: lock-mutex
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-lock-mutex:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: lockmutex-bucket
                  properties:
                    bucket: lockmutex
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: event-bus
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-event-bus:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
        - type: link
          properties:
            name: default
            namespace: wasi
            package: keyvalue
            interfaces: [store, atomics]
            target:
              name: keyvalue-nats
              config:
                - name: eventbus-bucket
                  properties:
                    bucket: eventbus
                    cluster_uri: nats://nats.wasmcloud.svc.cluster.local:4222
                    enable_bucket_auto_create: "true"
    - name: static-assets
      type: component
      properties:
        image: oci://registry.wasmcloud.svc.cluster.local:5000/vet-static-assets:0.1.0
      traits:
        - type: spreadscaler
          properties:
            instances: 1
    - name: httpserver
      type: capability
      properties:
        image: ghcr.io/wasmcloud/http-server:0.23.1
      traits:
        - type: link
          properties:
            namespace: wasi
            package: http
            interfaces: [incoming-handler]
            source:
              config:
                - name: listen
                  properties:
                    address: 0.0.0.0:8081
            target:
              name: vet-domain
    - name: httpclient
      type: capability
      properties:
        image: ghcr.io/wasmcloud/http-client:0.13.0
    - name: keyvalue-nats
      type: capability
      properties:
        image: ghcr.io/wasmcloud/keyvalue-nats:0.3.1
