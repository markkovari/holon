{
  "name": "e2e",
  "version": "1.0.0",
  "description": "``` cargo build --release --manifest-path host/Cargo.toml cargo nextest run --release --manifest-path reconciler/Cargo.toml -E 'test(six_manifests)' ```",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
