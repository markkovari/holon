# Clipboard Sync

This is a showcase application that fronts the `desktop-clipboard` capability over HTTP
(`components/clipboard-sync-domain`). It bridges the WebAssembly sandbox to the host
environment natively.

![Clipboard Sync Demo](../media/clipboard-sync.gif)
