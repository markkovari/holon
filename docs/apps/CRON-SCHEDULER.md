# Cron Scheduler

This is a showcase application that utilizes the `cron-scheduler` capability.
It bridges the WebAssembly sandbox to the host environment natively.

![Cron Scheduler Demo](../../docs/assets/cron-scheduler.gif)
