# Cron Scheduler

This is a showcase application that fronts the `system-cron` capability over HTTP
(`components/cron-scheduler-domain`). It bridges the WebAssembly sandbox to the host
environment natively.

![Cron Scheduler Demo](../media/cron-scheduler.gif)
