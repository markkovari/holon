# Docker Manager

This is a showcase application that fronts the `container-docker` capability over HTTP
(`components/docker-manager-domain`). It bridges the WebAssembly sandbox to the host
environment natively.

![Docker Manager Demo](../media/docker-manager.gif)
