# Notification Center

This is a showcase application that fronts the `ui-notifier` capability over HTTP
(`components/desktop-notifier-domain`). It bridges the WebAssembly sandbox to the host
environment natively.

![Notification Center Demo](../media/desktop-notifier.gif)
