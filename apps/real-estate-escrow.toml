# Generated by tools/gen-app-specs.py from this app's `host-real-estate-escrow` recipe.
# EDIT `domain` before deploying — the value below is a placeholder.
#
#   just selfhost-render real-estate-escrow            # read the unit and route first
#   just selfhost-deploy real-estate-escrow my-vps     # compose, ship, restart, route
name = "real-estate-escrow"
# tailnet is the default and is left alone on purpose: a generated file must
# never be the reason an app is reachable from the internet. Set
# access = "public" deliberately, per app.
domain = "real-estate-escrow.example.com"
artifact = "components/target/real-estate-escrow.composed.wasm"

# sqlite: one file under the unit's StateDirectory, so state survives the
# restarts `Restart=always` makes routine.
kv = "sqlite"
