# Generated by tools/gen-app-specs.py from this app's `host-freight-tracker` recipe.
# EDIT `domain` before deploying — the value below is a placeholder.
#
#   holon node render apps/freight-tracker.toml --out target/selfhost   # write the unit/env/route
#   then copy target/selfhost/freight-tracker/* to the box and `systemctl enable --now comp-freight-tracker.service`
name = "freight-tracker"
# tailnet is the default and is left alone on purpose: a generated file must
# never be the reason an app is reachable from the internet. Set
# access = "public" deliberately, per app.
domain = "freight-tracker.example.com"
artifact = "components/target/freight-tracker.composed.wasm"

# sqlite: one file under the unit's StateDirectory, so state survives the
# restarts `Restart=always` makes routine.
kv = "sqlite"
