# Generated by tools/gen-app-specs.py from this app's `host-academic-review` recipe.
# EDIT `domain` before deploying — the value below is a placeholder.
#
#   holon node render apps/academic-review.toml --out target/selfhost   # write the unit/env/route
#   then copy target/selfhost/academic-review/* to the box and `systemctl enable --now comp-academic-review.service`
name = "academic-review"
# tailnet is the default and is left alone on purpose: a generated file must
# never be the reason an app is reachable from the internet. Set
# access = "public" deliberately, per app.
domain = "academic-review.example.com"
artifact = "components/target/academic-review.composed.wasm"

# sqlite: one file under the unit's StateDirectory, so state survives the
# restarts `Restart=always` makes routine.
kv = "sqlite"
