# Generated by tools/gen-app-specs.py from this app's `host-health-records` recipe.
# EDIT `domain` before deploying — the value below is a placeholder.
#
#   holon node render apps/health-records.toml --out target/selfhost   # write the unit/env/route
#   then copy target/selfhost/health-records/* to the box and `systemctl enable --now comp-health-records.service`
name = "health-records"
# tailnet is the default and is left alone on purpose: a generated file must
# never be the reason an app is reachable from the internet. Set
# access = "public" deliberately, per app.
domain = "health-records.example.com"
artifact = "components/target/health-records.composed.wasm"

# sqlite: one file under the unit's StateDirectory, so state survives the
# restarts `Restart=always` makes routine.
kv = "sqlite"
